# Linux deployment uses relative /api paths.
# No frontend environment variable is required by default.
# Optional when deploying the exported frontend under a sub-path
# NEXT_PUBLIC_BASE_PATH=/trainerboard
